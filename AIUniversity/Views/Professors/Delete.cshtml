@{
  Layout = "_Layout";
}

@model AIUniversity.Models.Professor

<div class="card border-danger mb-3">
  <div class="card-header bg-danger text-white">Delete Professor</div>
  <div class="card-body">
    <h5 class="card-title">Are you sure you want to delete: @Html.DisplayFor(model => model.ProfessorFirstName) @Html.DisplayFor(model => model.ProfessorLastName)</em>?</h5>
    <p class="card-text">
    </p>
    @using (Html.BeginForm())
    {
      <button type="submit" class = "btn btn-danger mb-2">Delete</button>
    }
  </div>
</div>
@Html.ActionLink("Back to List", "Index", null, null, new { @class = "btn btn-secondary mb-2" })